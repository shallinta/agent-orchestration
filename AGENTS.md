# 项目协作规则

本文件约束参与本仓库工作的人工与 Agent。当前项目仍处于快速探索期，规则的目的不是增加流程负担，而是让每次判断、实验和变化都可追溯。

## 工作方式

1. 先理解问题和验证假设，再开始实现。
2. 每次只推进一个足够小的讨论或实施闭环；避免为了未来扩展提前建设抽象层。
3. 对影响产品边界、核心协议、状态模型或权限模型的决定，先讨论并获得用户确认。
4. 实现必须带有与风险相称的验证证据。不能把“代码已写完”当作“功能已完成”。
5. 作者与评审者尽量分离；重要方案和实现应由不同 Agent 交叉检查。
6. 用户保留关键节点的最终决定权。未经明确授权，不执行 push、PR、发布、部署、通知等外部副作用。

## Dreaming 阶段

在进入版本范围、技术方案或功能实现前，项目先通过长期、细粒度的 Dreaming 阶段理解自身要解决的问题。

Dreaming 阶段已于 2026-08-31 正式完成，记录范围为 Dreaming 0–54。此后不再创建 Dreaming 55，也不再使用 Dreaming 编号承载新的讨论阶段。未来认识可以在另行命名的新阶段中修正、扩展或替代 Dreaming 结论，但不得回写 Dreaming 历史记录；新阶段的名称和规则在开始时再确定。以下规则作为 Dreaming 0–54 的过程约束和历史阅读依据继续保留。

Dreaming 重点澄清：

1. 要解决的问题是什么，它是否是用户真实存在的问题。
2. 这个问题是否应当由本项目解决，而不只是“能不能解决”。
3. 解决问题时最理想的用户使用场景是什么，不因实现难度提前降低体验目标。
4. 使用场景涉及的产品概念、产品需求、领域关系、交互设计和交互流程，不讨论如何实现或是否容易实现。
5. 偶然想到的实现或模型设计思路可以先记入延后设计线索，但不在 Dreaming 中深入讨论。
6. 非核心的扩展功能想法可以先记入未来参考，不要求立即讨论或纳入本项目范围。
7. 允许问题暂时没有结论，或必须留到后续试验才能判断。此类事项应如实记录，不阻塞其他 Dreaming。

- Dreaming 使用从 1 开始的连续编号：`Dreaming 1`、`Dreaming 2`……
- `Dreaming 0` 是唯一的特殊编号，用于逐字保存用户最初的项目构想。它是历史输入档案，不表示全文已经成为共识。
- 一个编号只讨论一个最内核、足够小的思想点。讨论可以反复质疑、修改或推翻，不以尽快形成方案为目标。
- 一个 Dreaming 可以跨越任意多轮对话。不得把“一轮问答”机械地等同于“一个思想成果”；存在疑问、矛盾、未确认推导或更优可能时，应继续讨论当前编号。
- 讨论时，Agent 必须先说明自己理解了什么、理解依据来自哪里，并明确区分用户原始表达与 Agent 自己的推导。
- 用户与 Agent 的想法都不是天然正确的。Agent 不应只做顺从式整理，而应主动评价可行性、指出遗漏和矛盾、提出质疑或更好的替代方案，并与用户共同修正认识。
- 用户回复时通常只指出需要调整、质疑或补充的部分；用户未明确提及的上一轮内容，视为已经完全认同，不再重复确认。
- 不从尚未共同确认的推导直接跳到版本功能、架构选型或二选一决策。
- Dreaming 只讨论问题、需求、领域概念、期望行为、价值取舍与边界，不讨论技术实现。不得从领域名词推导代码对象，也不决定模块、类、接口、配置结构、命令、协议或调用流程。
- 当讨论自然触及“如何实现”时，只记录这是未来需要解决的问题，然后回到当前思想点；具体实现留到用户明确开启设计阶段后讨论。
- 只有在当前问题的关键分歧、边界和风险已经讨论充分，并形成双方都能清楚复述的可靠阶段性成果后，才将其写入 `docs/dreaming/`，关闭当前编号，然后递增到下一个编号。
- Dreaming 记录是项目认识在某个时刻的快照，不是不可修改的宪法。Dreaming 阶段结束后认识发生变化时，应在新的讨论阶段中新增记录，说明对 Dreaming 的修正、扩展或替代关系。
- Dreaming 一旦成文，不再因后续讨论而回写或调整其产品描述；旧记录永久保留当时的认识和术语。
- Dreaming 0–54 内部发生冲突时，总是以编号更大的 Dreaming 为准。未来新阶段的记录与 Dreaming 冲突时，应明确说明其修正或覆盖了什么，但不得改写旧记录。
- Dreaming 已经从最核心需求逐渐覆盖到支撑需求、边界与远期可能需求，供后续设计和迭代参考；这不代表所有记录都必须实现。
- 编号只表示思想成果首次成文的顺序，不表示永久优先级。未来可以在新阶段调整某项认识的重要程度或用新记录取代它，但 Dreaming 原文保持不变并保留变化原因。
- 在用户明确提出进入版本规划之前，不主动收敛 `v0.0.1` 范围，不编写实现计划，不初始化技术栈。

## 核心技术可行性调研阶段

核心技术可行性调研阶段于 2026-09-01 开始，记录统一保存在 `docs/technical-feasibility/`。当前阶段验证现有技术能否支撑 Dreaming 已确认的产品核心及其真实限制，不直接形成完整产品实现、版本范围或最终架构。

- 按阻断性门槛推进：先验证目标 Agent 的程序化与连续协作边界，再验证 Harness 公共能力通道和免人工中转闭环，最后研究恢复、并发、取消与保证强度。
- 每项调研必须先写明问题、当前假设、反证条件、验证方法和计划证据，再开始实验；不能先做技术原型再为它补写目的。
- 优先使用目标产品当前版本的官方资料、内置帮助和本机可复现实验。所有易变化结论必须注明日期、版本、环境和成立前提。
- 明确区分观察事实、分析推断、当前建议和用户已确认决定。实验跑通只证明相应条件下的可行性，不自动确定正式接口、模块、依赖或架构。
- 允许编写范围受控、可丢弃的探针和实验程序，但不得把调研代码伪装成生产实现，也不得为了复用提前建设通用框架。
- 成功与失败路径都要记录。单项结论使用`可行`、`有条件可行`、`不可行`或`未决`，并说明适用版本、环境、限制和时效性。
- 权限、审批、预算、取消、目录、网络和工具访问必须按真实证据标明强制来源、弱约束或未覆盖范围，不能以配置名称代替保证能力。
- 具体实验完成后同步单项记录与 `progress.md`；影响项目方向、范围或后续顺序的变化同时记入 `CHANGELOG.md`。
- 凭证、token、环境变量值、私有内容和未经脱敏的敏感输出不得进入命令记录、文档或 Git。
- 总计划先由用户审阅确认；确认前不开始 TF-01 及后续实验，不进入 `v0.0.1` 规划。

## Spark

- Spark 记录在 `docs/sparks/`，使用从 1 开始的连续编号：`Spark 1`、`Spark 2`……
- Spark 用于保存与 Harness 有关联、但不属于当前核心 Dreaming 的旁支灵感。
- Spark 不是已确认需求、产品路线图或实施承诺；后续可以继续讨论、修改或放弃。
- Spark 使用独立编号，不占用 Dreaming 编号，也不影响当前 Dreaming 的讨论与进展。

## 领域术语

- `thread`：由用户划定边界的持续协作上下文。它的生命周期与单个权威目标分离，可以跨越多个连续目标及目标完成后的保留期；当前目标完成不会自动结束，正式结束由具备相应 Thread 权限的平台用户手动执行。它不等同于聊天会话或固定时间段，也不必然等同于整个项目。
- `平台用户`：在 thread 之外通过 Harness 配套 UI、API 或其他平台入口进行操作的账号主体。平台用户不等同于用户身份卡、用户角色或主管理员。
- “身份资料卡”统一简称为“身份卡”。
- 当前身份卡分为两类：`Agent 身份卡`和`用户身份卡`。
- 身份卡是由平台用户负责提供和维护的声明性简历，Harness 不负责认证、评价或根据 thread 经历自动更新其内容。身份卡修改默认只影响此后创建的角色，不会自动改变既有角色；未来若允许调整既有角色，也必须是平台用户针对该角色作出的明确行为。
- `角色`：身份卡加入 thread 后产生的、只属于该 thread 的实例。它取代此前讨论中的“参与者”作为首选术语。
- `系统标签`：附加在角色上的系统语义标签，表示一定的系统权限与规则；它独立于 CEO、顾问、程序员、运营等场景化角色名、职位或身份。
- `主持`：唯一系统标签，表示该角色负责理解、组织和持续推进整个 thread 协作并完成目标；它取代此前讨论中的“主参与者”。当前共识中，主持附加在 Agent 角色上。
- `主管理员`：唯一系统标签，表示该角色在 thread 内拥有权威目标确认与最高审批等治理权。当前共识中，主管理员附加在用户角色上。
- `管理员`：非唯一系统标签，表示该角色在主管理员授权下局部负责 thread 中的某个领域；具体责任和边界尚未讨论。
- `初始意图`：主管理员在 thread 创建后用来启动实质协作的早期输入，可以是尚不完整、未结构化的问题、愿望、背景、材料或限制；它不是权威目标，但主持从收到它开始承担澄清、组织与持续推进责任。
- `探索性协作`：权威目标确认前，主持为减少未知、形成目标候选而组织的普通沟通、委托、资料查询或讨论；“服务于探索而非把未确认目标当作既定目标推进”是主持职责提示中的语义行为边界，不是 Harness 对内容进行分类后施加的专门门禁。
- `目标确认`：主管理员在 thread 内执行的明确、可追溯治理行为，使满足最低内容要求的目标成为当前权威目标；可以由主管理员一次性直接提交并确认，也可以先由主持整理候选再交给主管理员确认，不能从普通自然语言、沉默或后续行为中推断。
- `目标最低内容`：所有权威目标统一必须包含的两项语义内容，即希望实现的`预期结果`和届时用来判断目标已经达到、成果可以接受的`完成判断依据`；其他背景、范围、产物、限制、资料、风险与时间信息均按场景补充或引用，不是全局必填。
- `目标完成提议`：主持向主管理员提出当前权威目标可能已经达到的有依据说明；它至少连接实际结果与目标的完成判断依据，明确说明是否存在主持认为重要的未决风险、缺口或责任，并在存在时如实披露，但不会自行产生目标完成确认效力。
- `阶段推进安排`：Dreaming 33 曾用于描述主持面向近期工作的认识与安排的历史术语；按照 Dreaming 39，“阶段”不是平台或 Harness 的通用语义，当前应理解为主持形成足以继续推进的近期安排，thread 内角色可以用阶段、Step、其他表达或完全不命名。
- `推进语义`：thread 内角色用来命名和切分协作进程的场景化表达，例如阶段、Step、轮次或里程碑；它可以因 thread 而异或完全不存在，不会自动成为平台或 Harness 的系统概念。
- `委托`：具备相应发布权限的 Agent 角色要求一个唯一 Agent 目标角色完成工作并交付结果的、独立持续跟踪的工作责任。它与普通沟通消息分开；用户角色不能发布、承接或验收委托。
- `委托起步上下文`：Agent 委托发布者为帮助 Agent 目标角色可靠开始具体工作而提供的相关语义信息与查询线索；它按场景组织，不要求复制完整 thread，也不能被目标角色的主动查询能力所替代。
- `结果吸收责任`：Agent 委托发布者理解目标角色交付的结果、将其带回自身工作，并把影响整体推进的重要内容明确同步主持的责任；它不同于委托验收，也不会因结果已经进入完整记录而自动完成。
- `最低限度成果痕迹`：Harness 不持续记录角色在关联工作空间中的每次文件、工具或云端状态变化，只保留最终成果交付、委托状态、必要角色消息以及 Harness 自己直接参与的外部操作事件；成果默认由委托发布者吸收，影响整体推进的重要内容再同步主持。记录成果痕迹不会自动产生质量认可、委托验收、目标完成或治理效力。
- `独立评审`：由不同于原成果生产者的 Agent 角色通过单独委托，对既有成果重新进行检查并交付评审意见的工作；它按明确要求或语义风险发生，不是所有成果的默认步骤，也不自动转移原委托的责任或验收权。
- `委托终态`：一项委托的可跟踪工作责任已经结束并永久冻结的状态；Harness 保证它不能重启或改回非终态，但这不会关闭相关话题、角色关系或普通消息沟通，后续新责任需要另行发布委托。
- `目标完成后的委托收束`：主管理员确认权威目标完成后，主持审视受影响的未终结 Agent 委托，并通过各自发布者明确继续、调整、取消或暴露未决问题的语义责任；目标完成本身不会级联改变任何委托状态。
- `无当前权威目标期间`：一个权威目标已经完成且新的权威目标尚未确认时，thread 继续存在的客观情形；它不是平台或 Harness 的阶段，主持只提供有限后续选择，不自主开展猜测性实质工作。
- `主持主动静默`：无当前权威目标时，主管理员明确不需要后续动作后，主持停止自主提出方向和启动新工作的语义行为边界；它不暂停 thread，也不取消既有责任或屏蔽确定性触发，已有事件只会触发必要响应，处理后在没有新输入时继续静默。
- `定时任务`（暂称）：在时间条件到达时，要求目标尝试完成一项已经安排好的工作；它可以一次或重复触发，可以面向 Agent 角色发送执行输入，也可以面向用户角色发送普通提醒。
- `心跳检测`（暂称）：周期性唤起 Agent 角色检查当前状态并判断本次是否需要行动的能力；“无需行动”是正常结果，用户角色不作为心跳检测的直接目标。
- 定时任务与心跳检测不形成统一的“持续活动”上位概念；二者以“到时做”和“到时检查是否要做”的用户意图区分，不以一次或周期、Cron 或 interval 等实现形式区分，最终名称仍未决定。
- `用户角色 Channel 分发`：一条私聊或广播消息只要明确把某个用户角色列入接收范围，Harness 就把它发往该角色已经连接的通信 Channel；是否发送由主持或相关角色判断，Harness 不按内容重要性或回应期待二次过滤。消息发送不自动产生回复义务，用户沉默也不产生确认、批准或其他治理含义。
- `截止要求`：作为委托条件被明确、机器可识别地发布的时间约束；委托合法生效时同步生效，供 Harness 确定性监督。它不同于只供排序与计划判断的期望时间或时间建议，也不自动产生跨工作排序权。
- `调用故障`：Harness 在调用边界无法可靠启动或连接 Agent、恢复所需会话，或取得可作为本次调用结果使用的可靠返回等客观故障。它不同于 Agent 正常返回的“无法完成或受阻”工作结果，也不自动等于委托失败。
- `进展快照`：Agent 或真人基于完整 thread 记录生成的语义派生工作视图，用于呈现某个检查点的协作进展；它不能替代事实来源，可能过时、遗漏或包含角色判断。
- `知识检索层`：以完整 thread 记录为事实来源、由 Agent 或真人持续进行语义整理，并向所有角色提供权限受控查询的派生知识视图；它区分当前适用知识与历史演变，不替代完整记录，也不自动创造权威结论。
- `入场上下文`：新角色加入已有 thread 时获得的分层、权限受控的协作信息入口；它帮助角色开始当前工作，但不替代完整记录、不自动扩大角色权限，也不保证其中的语义整理绝对完整或正确。
- `资源预算`：由具备相应 Thread 配置权的平台用户设置、由 Harness 对能够确定性计量和控制的范围强制执行的 thread 自治资源边界；它约束已明确覆盖的资源消耗，不只是事后统计。Agent 通过 Harness 无法计量或阻止的原生能力产生的成本不自动受同等硬上限保证，产品必须公开预算覆盖范围；Harness 也不据此判断调用是否值得。
- `自治程度`：角色在特定 thread 中可自主行动的实际范围，由角色权限、审批策略和资源预算三项正交约束共同形成；它不是替代三者的单一等级，也不是第四套授权。
- `强制约束`：在限制条件不满足时，能够由 Harness 或明确、可靠的外部机制实际阻止行为的约束。产品必须说明保证来源与覆盖范围；原有强制边界不可用时不能静默降级。
- `弱约束`：通过提示词、角色说明或协作规则要求 Agent 遵守、但不能保证实际阻止违规行为的行为限制。弱约束可以有实际价值，但必须与强制权限、审批和硬预算的保证措辞明确区分。
- `未覆盖能力`：Harness 不知道、无法观察或无法约束，因而没有给出强制保证的 Agent 原生能力范围。未覆盖不等于禁止、允许、安全或已经发生风险；产品应当如实披露这一边界。
- `免协调连续推进能力`：在角色权限、审批策略和资源预算允许时，Harness 支持主持与其他 Agent 角色在无需真人逐轮转发和推动的情况下连续协作，使用户可以暂时离开并在关键判断点再介入。这是产品必须提供但不要求每个 thread 实际出现的核心能力，不是系统状态、阶段、模式或程序化验收指标。
- `领域无关核心`：Harness 的通用产品语义不预设软件开发或其他单一领域的阶段、职位与产物模型；软件开发是最重要的初始使用和验证场景，可以获得领域便利，但不是所有 thread 的强制流程。领域无关不表示首版必须支持所有行业，也不表示产品已经具备各领域所需的专业可靠性、安全性或合规性。
- `访问入口`：UI、API、命令行或消息 Channel 等访问同一 Harness thread 的产品入口。不同入口可以只提供部分能力，且不会各自拥有或复制一份权威 thread；入口本身也不创造平台用户权限、角色权限或 thread 内治理权。
- `可参与 Agent`：能够通过可靠的程序化边界被调用、维持 thread 角色所需的逻辑连续性，并独立承担 Agent 级工作后返回可用或明确受阻结果的完整接入能力。资格不取决于 CLI、远程服务、桌面 App 等产品形态；裸模型 API 不会仅凭文本输入输出自动获得资格，Harness 也不负责为其补齐通用 Agent 循环。
- `Harness 公共协作能力`：由 Harness 提供、供角色读取或改变 thread 协作事实的明确能力，例如发消息、发布委托、查询 thread 和申请审批。Agent 的内部工具循环与原生环境能力不因此被 Harness 全量代理；MCP 只是未来可能的承载方式之一，不是当前产品边界的一部分。
- `角色可用状态`：Harness 根据可客观确定的运行事实，表达一个角色当前能否接收普通后续工作的状态；它不表示角色的意愿、能力、优先级或语义上的适合程度。
- `能力阻塞`：主持基于工作内容和当前人才范围作出的语义结论，表示没有合适的已有角色、主持也无法负责任地自行执行，且当前配置不允许主持通过引入新角色申请来推进；它必须被显式报告给主管理员，不是 Harness 从角色可用状态自动推导出的运行事实。
- `角色停用`：具备相应 Thread 权限的平台用户对现有 thread 角色施加的可逆运行限制；停用角色仍属于 thread 并保留身份与历史，但不接收新的普通消息或委托，也不产生新的调用资源，重新启用后仍是同一逻辑角色。
- `优雅停用`：默认的角色停用方式；立即阻止角色接收和开始新的工作，允许已经开始的在途执行完成并记录结果，待在途执行结束后进入完整停用状态。
- `强制停用`：平台用户明确选择的角色停用方式；立即阻止新的工作，并要求尽快停止在途执行，但不承诺撤销已经产生的结果、成本或外部副作用，也不把无法确认的执行结果伪装成确定结论。
- `角色并发能力`：一个角色同一时刻可主动执行工作的数量边界；不同角色可以不同，该能力本身默认不限制角色接收并持有待处理普通消息和委托的数量。
- `待处理内容`：角色已经接收、但尚未开始主动处理的普通消息或委托的集合性称呼；它不把两者合并成统一数据实体或队列，也不改变各自的语义和生命周期。
- 身份卡是可复用模板；角色及其系统标签只在特定 thread 内成立，不是身份卡永久拥有的属性。
- 角色的行为与权限只发生在 thread 内；平台用户在 UI/API 中拥有的操作权属于平台账号体系，不能由 thread 内系统标签自动推出。Thread 配置的实际设置与修改权属于具有相应 Thread 权限的平台用户，不属于主管理员角色。

## 记录要求

- 新想法、方案转向、实验结论、功能实现和明显问题都记录到 `CHANGELOG.md`。早期采用手记式表达，优先记录“为什么变化”。
- 每个 thread 或阶段性讨论可在 `docs/project-log/` 下维护独立记录。
- 已确认且影响长期方向的决定写入 `docs/decisions/`；不要把尚未确认的设想伪装成决策。
- 文档必须区分：已确认事实、当前假设、待验证问题和明确非目标。
- 代码与文档变更应保持同步；如果行为变化但文档未更新，任务不算完成。

## 产品认识边界

- 用户最初描述的愿景、现有项目文档和 Agent 的整理都可以被继续讨论、优化或推翻，不能自动视为固定需求。
- `README.md` 和 `docs/project-log/` 中的早期产品内容是初始理解快照。只有经过 Dreaming 讨论形成的阶段性共识，才进入 `docs/dreaming/`。
- Agent 不得把自己的分析建议写成用户已经确认的事实；文档必须保留信息来源和确定性边界。
- 默认采用最小权限。凭证、环境变量值和敏感输入不得写入日志或提交到仓库。

## Git 约定

- 提交应小而聚焦，提交信息首先说明意图和原因。
- 不覆盖、不清理用户已有但无关的工作区变更。
- 不使用破坏性 Git 命令处理不明确的状态。
- 版本从 `v0.0.1` 开始；只有形成可运行、可验证的闭环后才创建对应 tag。
