# 项目手记

这里记录项目从想法到方案、从实验到实现的变化。早期不采用正式软件发布日志格式，而是尽量保留当时的问题、判断、理由和未决事项。等项目进入稳定发布阶段后，再决定是否拆分出标准化 changelog。

## 2026-08-12：项目起点

最初的痛点很具体：开发一个需求时，Codex 擅长全面分析、方案设计和质量评审，但执行速度较慢；Claude Code 接入速度快的模型后很适合在充分约束下完成编码，却不一定适合独立做复杂方案和评审。人工可以组合两者的长处，但需要不断复制方案、分派任务、转发评审意见、催促修复和整理进展，自己反而变成了消息中转站。

项目希望把这段搬运工作交给一个 harness：由主 Agent 负责理解需求、推进流程和安排协作，Harness 负责可靠地传递结构化上下文、调用 Agent、记录事件、控制并行与外部副作用，并在关键节点把决定权交回用户。

长期设想包括 Agent 身份资料卡、CLI 与远程适配器、thread 生命周期、交叉评审、多 Agent 讨论、进展检查点、外部通知、UI 协议以及跨需求知识沉淀。它们先作为方向存在，不代表都会进入早期版本。

今天确认采用敏捷方式推进，从 `v0.0.1` 开始，只做最小真实闭环，通过实际使用获得反馈后再扩展。前端 UI 暂后，Harness 核心必须独立于具体界面。

仓库进行了第一次文档型初始化：建立 Git、本地提交身份、项目说明、协作规则、项目手记、立项记录和决策记录目录。此时没有选择语言或技术栈，也没有创建产品代码。

初始化复核后，决定把 `.omc/` 与 `.superpowers/` 作为本地讨论和编排运行状态整体忽略；需要长期保留的结论应整理进正式项目文档，而不是依赖工具内部状态。

接下来最重要的问题是：`v0.0.1` 究竟要验证哪一段协作链，以及流程推进权在确定性的 Harness 与主 Agent 之间如何分配。

## 2026-08-12：先 Dreaming，再谈版本

第一次尝试讨论时，过早从整体构想跳到了 `v0.0.1` 的流程控制权选择。这个推进方式忽略了一个前提：现有内容只是可以持续修改的初始想法，我们甚至还没有共同确认项目最底层的问题是什么。

因此调整合作方式：先进行长期、细粒度的 Dreaming。每次只围绕一个足够小的思想点讨论，使用从 1 开始的连续编号；只有形成清晰、可复述的阶段性成果后才落文档并递增编号。在用户明确提出以前，不主动进入版本功能、架构选型或实现计划。

同时从 `AGENTS.md` 移除了过早固化的具体架构倾向。初始构想、已有文档和 Agent 推导都不是项目宪法，它们可以在后续讨论中被修正或推翻。

## Dreaming 1：人被迫填补 Agent 之间的协作空隙

第一个被共同确认的核心问题不是“缺少一个更聪明的 Agent”，而是多个各有优势、已经足够有用的 Agent 无法自然地共同维持一段连续工作。为了组合它们的能力，人需要持续理解输出、重组上下文、编写下一轮提示、转交工作、跟踪进展并决定何时继续。

项目最原始的出发点由此被表述为：人被迫填补多个 Agent 之间的协作空隙。Harness 是否以及如何接管这些空隙，仍留给后续 Dreaming 逐项讨论。

同时确认 Dreaming 会从核心需求逐渐延伸到未来可能需求，形成后续讨论和迭代的参考集合。编号表示成文顺序，不是永久优先级；记录随时可以更新、删除、调整在索引中的重要程度或被新的认识取代，但不改写历史编号。

## Dreaming 0：保存最初构想

应用户要求，将项目讨论开始时的完整构想逐字保存为 `Dreaming 0`。它是防止起点被遗忘或注意力稀释的历史输入档案，不参与思想成果的递增顺序，也不表示原文中的每项设想都已经确认。

## Dreaming 2：人从持续协调者转变为关键判断者

进一步确认项目不是以消灭人的参与为目标。希望减少的是整理上下文、改写提示词、消息转发、催促和进展维护等持续协调劳动；人仍在需要价值判断、风险接受和最终负责的关键节点介入。

人的理想位置由此表述为：从持续在线的协调者，转变为关键节点的判断者和最终责任人。具体哪些节点需要人、如何识别和呈现，尚未讨论。

## Dreaming 3：组合不同 Agent 的比较优势

确认多 Agent 的价值不在数量或形式，而在组合不同参与者的比较优势。这些优势不只有质量、速度和擅长领域，调用价格也是重要维度；理想目标是在特定用户、任务与约束下，取得更好的质量、速度、成本和能力覆盖组合。

同时修正了一个可能的误读：Codex 负责方案、Claude Code 负责执行只是一个具体使用案例，不是默认规则。即使是同一种 Agent，也会因模型、插件、长期记忆和其他配置而表现不同。Agent 特征应容纳用户基于真实使用形成的判断，并允许这种判断继续变化。

## Dreaming 4：Harness 调度的是项目参与者

身份资料卡的含义进一步扩展：它不是 Agent 产品说明，而是一个项目参与者在协作中的身份表示。身份背后既可以是带有模型、配置、记忆和环境的数字 Agent，也可以是具有老板、产品经理、编辑等身份的真人；远期甚至可能是以“仓管员”等身份参与的系统平台。

由此确认 Harness 在 thread 中实际调度和协调的是项目参与者，而不是直接面对 Agent 产品或用户账号。多 Agent 和真人可以通过同一层参与者抽象进入协作，但不代表它们具有相同的能力、权限或调用协议。

多人多 Agent 编辑出版、真人与 NPC 共同游戏、外部系统承担项目职责等例子被保存为远期启发，不进入当前范围。`agent-orchestration` 名称可能偏窄的问题也暂作旁注，等项目本质更加清晰后再考虑。

## 2026-08-12：Dreaming 不以对话轮次计数

进一步修正讨论方式：一个 Dreaming 可以跨越多轮对话，不能因为用户回答了一次就立即成文并进入下一个编号。只要仍有疑问、矛盾、未确认推导或值得比较的替代方案，就继续停留在当前问题。

同时明确用户和 Agent 的想法都不是真理。Agent 的职责不只是准确整理用户表达，还包括提出评价、质疑、遗漏、现实风险和更优方案；直到双方形成更可靠、更实际的阶段性认识，才记录思想成果。

## 2026-08-12：Dreaming 不讨论技术实现

讨论“项目参与者”时，过早把领域概念映射成了具有公共接口和内部管线的实现对象。实际上，项目参与者未来既可能是含初始化信息的数据，也可能由独立 Executor 执行动作；Dreaming 阶段不应选择其中任何一种。

因此进一步明确边界：Dreaming 只讨论问题、需求、领域概念、期望行为、价值取舍和范围边界，不讨论模块、类、接口、配置、命令、协议或调用流程。实现问题只作为未来待解决事项保留，等用户明确开启设计阶段后再展开。

## Dreaming 5：身份资料卡是模板，参与者是 thread 内实例

确认身份资料卡是长期保存、可反复选用的“人才简历”，并不是长期存在的参与者。资料卡每次加入 thread 都产生一个只属于该 thread 的独立参与者；同一张资料卡可以在不同 thread 或同一个 thread 中多次使用。

不同参与者分别保持连续且相互隔离的工作上下文。即使它们使用同一种底层 Agent，也可以通过不同身份专注于代码、绘图等不同工作，避免不相关任务破坏上下文连续性。参与者的逻辑生命在 thread 内持续，thread 结束时结束；跨 thread 记忆与经验另行沉淀，不默认延续整个旧参与者。

当前只聚焦数字 Agent 与真人两类参与者，系统平台参与属于远期启发。讨论中出现的声明式接入与可编程接入两级扩展想法另存为延后设计线索，当前不作技术选择。

## 2026-08-13：明确 Dreaming 要回答什么

Dreaming 不只是抽象概念讨论。它需要逐步确认问题是否真实、是否应由本项目解决、最理想的用户使用场景，以及相关产品概念、需求、领域关系和交互。实现难度不能提前压低体验目标；偶然出现的实现思路和扩展功能想法可以另行记录但不深入。允许部分问题没有结论，留待后续试验。

## Dreaming 6：Thread 是一项有界协作

确认 thread 是一项具有共同目标和预期成果、有开始、有进展、有结束的有界协作，而不是聊天会话、整个项目或固定时间段。用户从创建 thread 开始使用 Harness，并自行决定工作的切分粒度；Harness 可以提供文本式启发建议，但不约束或代替用户判断。

本 Harness 聚焦一个 thread 生命周期内的协作。Thread 结束时可以把经验、知识和业务资料以文档、知识库或其他产物交给用户，但跨 thread、跨项目的信息管理不属于其职责。相应能力被记录为未来独立的 Agent 项目管理工具方向。

## Dreaming 7：每个 Thread 同时只有一名数字主参与者

确认每个 thread 在任意时刻有且只有一名由用户指定的数字主参与者。主参与者承担协作主持人或 PMO 式的整体推进职责：理解目标，拆分和分派工作，吸收结果，判断下一步，识别用户决策点，并在无需用户介入时持续推动协作。

主参与者是叠加在身份资料卡之上的 thread 内职责，不是一种永久业务身份，也不代表最高权力。同一张资料卡在其他 thread 中可以只是普通参与者；真人老板、产品经理、游戏主持人等仍可承担业务角色，用户继续保留关键判断和最终责任。

用户可以在协作中更换数字主参与者，既有 thread、目标、进展和参与者关系不因此重建。真人主参与者因复杂度较高、需求真实性和规模尚未验证，被保留为远期扩展可能；当前候选场景优先由数字主参与者与真人业务主持人共同覆盖。提示词、交接机制和权限实现均未在本次 Dreaming 中决定。

## 2026-08-17：身份卡、角色与系统标签

进一步明确领域术语：身份卡加入 thread 后产生的实例称为“角色”；“主持”“主管理员”“管理员”等是可附加在角色上的独立系统语义，统一称为“系统标签”。系统标签可以组合，并各自拥有适用资格、唯一性、权限和规则约束；它们不等同于 CEO、顾问、程序员等场景化身份。

## Dreaming 8：Thread 的创建、治理与权威目标

确认创建 thread 时必须从 Agent 身份卡产生一个 Agent 角色并附加唯一主持标签，同时从已有或新建的用户身份卡产生一个用户角色并附加唯一主管理员标签。主管理员不是唯一真人用户角色；其他初始身份卡可选，角色也可以在协作中按需加入。

Thread 必然拥有影响本次协作的配置，但用户可以接受默认配置而不逐项设置。当前只确认工作目录、权限、审批与自治、轮次推进、等待超时、并发、失败恢复、统计预算等产品配置类别，不决定具体表达和实现。

Thread 可以在权威目标确认前存在，创建时无需提交完整需求。目标、背景、资料、规则、限制、预期成果和协作方式可以由所有角色在 thread 内共同提出、质疑和完善；只有主管理员能够确认当前权威目标，后续每次目标更新也必须由主管理员确认。主持负责持续推进，主管理员拥有 thread 配置、目标确认和最高审批等唯一治理权。

默认情况下，主持发现缺少合适角色时向主管理员申请引入；是否允许配置为自主引入仍未确认。系统标签可以在符合各自资格和规则时组合：主持唯一且当前只附加在 Agent 角色，主管理员唯一且当前只附加在用户角色，管理员非唯一并由主管理员设置，其具体适用资格、责任与权限范围留待未来讨论。

同日将 README 与 Dreaming 4–7 中已经过时的“身份资料卡”“项目参与者/参与者”“主参与者”等首选术语同步为“身份卡”“角色”“主持”。历史手记保留原文，通过本条说明术语演进。

## 2026-08-18：后续 Dreaming 覆盖旧结论，不回写旧记录

从 Dreaming 9 开始，每份 Dreaming 永久保留成文时的思想与术语，不再因后续讨论修改旧文档。后续记录与旧记录冲突时，总是以编号更大的 Dreaming 为准，并在新记录中说明修正或覆盖关系。这样既保留认识演进的历史，也避免反复维护所有旧文档。

## 2026-08-18：未提出异议即视为认同

后续讨论中，用户只需回复需要调整或澄清的内容；未明确提及的上一轮理解视为已经完全认同，不再逐项重复确认。同时新增“平台用户”术语：它是在 thread 外通过 UI/API 操作 Harness 的账号主体，不等同于用户身份卡、用户角色或主管理员。角色权限由平台用户设置，thread 内系统标签不能自动产生平台层操作权。

## Dreaming 9：消息路由、完整记录与权限分层

确认 Harness 完整持久化 thread 生命周期内的所有角色消息、事件和信息，定向发送不影响记录完整性。配套 UI 展示这份完整记录，本地数据保存在创建 thread 的机器上；谁能通过 UI 查看或管理它，取决于平台用户对机器、UI、远程鉴权和 thread 的访问权限，而不取决于是否拥有 thread 内用户角色。飞书等外部消息通道只向具体用户角色背后的真人送达定向消息，不开放完整 thread 历史，相关 gateway 与通道建设暂不深入。

Thread 默认不广播。主持分派任务时明确目标角色、推荐查询的消息或上下文关键字、允许讨论的对象角色和结果回报角色；目标可以是单个、多个或全员，未指定回报对象时默认回主持。没有合适角色时，主持先向主管理员申请新增，或请有权限的管理员指定。主持推荐相关上下文，但不为单次任务临时限制查询范围；角色在自身权限内主动查询消息、thread 上下文或知识库。

Thread 可以切换为适合脑暴或会议的全员广播，并在生命周期中随时调整；单条消息也可以定向给多个角色或全员。完整记录可以供外围 thread 上下文查询能力和知识库持续维护并向角色提供查询，具体实现延后，查询范围始终受角色权限约束。

进一步区分平台用户、Thread 权限与角色权限：平台用户属于 UI/API 层，Thread 权限决定平台用户能否访问或管理完整 thread，角色权限决定角色在 thread 内能使用哪些本地项目文件、技能、工具或服务、审批、消息与上下文查询能力。角色权限由具备相应操作权的平台用户在角色加入时设置并可调整；同一个人可以同时是平台用户和用户角色背后的真人，但两者不是同一概念。

本轮明确修正 Dreaming 8：所有 Thread 配置的实际设置和修改权属于具备相应 Thread 权限的平台用户，不属于主管理员角色。主管理员只保留 thread 内目标确认、最高审批等治理权。Thread owner 或 creator 通常与主管理员背后的真人相同，但不强制绑定；旧记录保持原文，以 Dreaming 9 为准。

## 2026-08-20：新增 Spark 旁支灵感记录

新增独立于 Dreaming 编号的 Spark 机制，用于保存与 Harness 有关联、但不属于当前核心 Dreaming 的旁支灵感。Spark 不是已确认需求、路线图或实施承诺，可以继续讨论、修改或放弃，也不影响当前 Dreaming 的进展。

Spark 1 记录了一个 Agent 猎人公会式任务委托系统：Agent 注册账号，在委托板上发布、查找和领取带有难度、领域特征、要求与积分回报的委托，提交完成结果，并通过积分升级、解锁更高等级委托和参与排名。它是在 Harness 之外考虑的旁支系统设想，未来也许能与 Harness 配合，或作为 Harness 委托模块的一种实现框架或参考；具体关系尚无结论，目前不属于 Harness 已确认需求或实施方案。

## 2026-08-20：补充 Spark 1 的多人承接语义

Spark 1 补充一项委托属性，用于明确是否允许多个 Agent 重复承接。允许时，每个承接者分别独立完成并提交完整委托，不表示把工作拆分给多人或由多人协作完成；多人承接下委托整体状态如何判断仍未讨论。

## Dreaming 10：委托是独立跟踪的工作责任

确认普通消息只承担沟通，不天然产生需跟踪的责任；Harness thread 内的“委托”是发布角色要求一个唯一目标角色完成工作并交付结果的、独立持续跟踪的工作责任。委托必须以机器可识别的结构化形式发布，结构化消息进入完整会话记录，委托本身另有独立持久化记录并与消息关联。

委托合法发布后立即生效，无需目标角色预先接受；目标角色可以拒绝或请求调整，发布者可以取消。委托可要求发布者验收：无需验收时，目标角色标记完成即进入终态；需要验收时，标记完成只代表交付，发布者验收后才结束。精确状态和何时需要验收尚未决定。

一项委托恰好只有一个负责角色。同一工作要由多个角色独立完成时，分别发布内容相同、状态独立的多项委托。发布委托属于角色权限，主持默认拥有，其他角色的发布范围由具备相应操作权的平台用户配置；受托角色继续委托他人不会转移原责任。发布者固定拥有验收权，Dreaming 9 的回报角色只接收消息，第三方评审需另建评审委托。本轮据此细化并覆盖 Dreaming 9 中可能把多目标任务消息等同于委托的理解，同时明确 Spark 1 的多人重复承接是其旁支系统设想中的委托板语义。

结构化委托消息与独立记录谁先创建，保留两种延后设计线索，本轮不选择；精确 schema、协议、工具、数据库表、状态枚举、验收要求的默认值与适用判断、具体通过标准及委托关系均未决定。

## Dreaming 11：语义判断属于角色，Harness 负责确定性执行

确认 Harness 是不承担意图识别的确定性程序。理解自然语言、评价结果含义与质量、选择业务下一步由 Agent 或真人承担；真人在 thread 内通过用户角色作出这些判断，平台用户通过 UI/API 发出的明确配置操作则是无需猜测的显式指令。

主持负责理解整体目标、评估结果、决定下一项委托、识别用户决策点，并判断何时可以提议完成 thread，但不能据此单方面结束 thread。Harness 只负责结构化信息与权限校验、消息和事件路由、持久化，以及按照明确配置执行并发、等待与超时、失败重试、审批门禁和定时触发等确定性工作。

Harness 可以识别委托超时、调用失败、无活跃委托和检查点到达等客观事实，但不能判断方案是否足够好、目标是否实现或下一步采取什么业务行动。自动策略必须有明确的条件、次数、时间和权限边界；策略完成、耗尽或遇到未覆盖情况后，Harness 持久化并报告失败原因、次数、状态等事实，不无限处理，也不自行编造解决方案。

委托级事件默认发给委托发布者并按明确规则或配置同步目标角色，thread 级事件默认发给主持，审批事件发给配置指定的审批角色；Thread 配置可以显式覆盖默认路由。Harness 只依据事件类型、对象归属和配置，不判断谁最适合解决；需要主管理员决策时，由主持或相关角色提出。所有事件继续按照 Dreaming 9 进入完整持久化记录。

策略表达、次数默认值、事件分类全集、路由配置、停滞判定、检查点细节、通知渠道及模块、接口、协议和状态机均未决定。

## Dreaming 12：Thread 的生命周期可以跨越权威目标

进一步区分三个动作：主持可以提议当前目标与成果已经完成；主管理员在 thread 内确认权威目标已经达到、成果可以接受；具备相应 Thread 权限的平台用户可以手动正式结束 thread。三者互不等同，目标完成不会自动结束 thread，当前也不考虑配置自动结束；平台用户可以选择长期不结束。

目标生命周期与 thread 生命周期由此分离。当前目标完成后，thread 可以继续用于查询历史、长期跟踪未来发展，或继承已有上下文后讨论和确认新目标；也可以继续发生消息和委托，当前不强制先形成新目标。一个 thread 可以先后经历多个权威目标，也可以处于目标完成后的保留期或无活跃工作阶段，具体状态名称尚未决定。

权威目标的提出、讨论、设置、更新、替换和完成确认属于 thread 内语义治理：所有角色可参与，主持负责组织和提示，主管理员最终确认；它不是平台用户修改 Thread 配置。新旧目标是否相关、是否建议另建 thread 也属于语义判断，只能由主持在 thread 内提示，Harness 或平台入口不依据固定规则自动提示。主管理员决定是否在当前 thread 确认新目标；选择另建时，由具备权限的平台用户执行创建。

用户仍拥有 thread 切分权，主持只建议不强制。上下文污染和角色职责错位可以由主持提示，但不固化为 Harness 规则。本轮明确修正 Dreaming 6：thread 不再与一项有开始、有结束的有界协作及其目标严格绑定，而是用户划定边界、可跨多个连续目标和保留期的持续协作上下文；它仍不等同于聊天会话、固定时间段，也不必然等同整个项目。

与 Dreaming 11 一致，Harness 不判断目标完成或新旧目标关联，也不自动结束。关闭 UI、自动结束、状态枚举、保留期资源与会话、目标历史结构、重设目标流程，以及结束后的只读、恢复或归档行为均未决定。

## Dreaming 13：进展快照是完整记录的语义派生视图

确认完整消息、事件和委托记录是 thread 的事实来源；进展快照是 Agent 或真人基于事实生成的语义派生工作视图，可能过时、遗漏或含有角色判断，不能覆盖、修改或替代完整记录。快照可以包含当前权威目标、已完成和进行中的工作、重要结论、风险、待决策事项与建议下一步，但固定模板尚未决定。

主持默认负责确保快照更新及时并可支持协作，可以亲自生成，也可以通过普通委托请其他角色代写。Harness 只触发“生成新快照”的请求、提供既定检查点范围内的新增事实、持久化并展示快照，不概括事实、不判断重要性，也不评价质量。

一般情况下，每次检查点产生一份新快照，历史快照不回写；thread 提供稳定的“当前进展”入口指向最新快照。旧快照有误时在后续快照说明修正或取代，旧记录仍保留。无变化时是否仍生成快照尚未决定；这项产品行为也不决定文件数量、数据库版本或其他存储实现。

检查点可以来自主持、有权限角色或具备相应 Thread 权限的平台用户的明确请求，也可以来自按时间、明确事件、数量阈值等候选条件配置的确定性触发。Harness 不根据内容重要性自行触发，只把检查点作为客观事件并请求 Agent 或真人生成语义快照；默认触发、权限和事实范围仍未决定。

快照无需主管理员逐次验收，也不会自动成为权威结论。代写时可以使用普通委托验收，但不增加额外系统审批，默认是否验收尚未决定。快照应区分权威目标与已确认决定、客观事实、角色判断、风险和待确认问题；其中写明“已完成”或“已确认”只能反映此前已经发生的治理行为，不能由快照文字自行创造。主管理员只在涉及目标变化或其他治理事项时介入。任何角色都可质疑快照，并通过后续快照纠正；内容应可追溯至消息、事件、委托或旧快照，具体引用形式未决定。

本轮与 Dreaming 9 的完整记录/定向展示分离和 Dreaming 11 的确定性 Harness 边界保持一致。历史快照不回写是用户独立确认的产品行为，不是由当前仓库 Dreaming 文档规则推导出的实现要求。快照字段与格式、存储、稳定入口、版本和引用结构、触发默认值与权限、UI/API/文件布局及自动质量评价均未决定。

## Dreaming 14：新角色通过分层入场上下文进入已有 Thread

确认角色可以在 thread 生命周期中随时加入，而 thread 又可能长期存在并跨越多个权威目标。这里的“随时”不改变既有角色引入权，也不表示主持已获得自主引入权；入场上下文不会让新角色继承旧角色身份、记忆或会话。将全部历史一次性提供给新角色会引入大量噪声，只提供最新进展快照则会把可能过时、遗漏或含有判断的派生视图当成唯一依据，因此两者都不适合作为默认入场方式。

新角色默认通过分层入场上下文获得协作起点：当前权威目标，若当前无活跃权威目标则如实说明，只有确在讨论时才标明正在讨论；已经确认的关键决定；若已存在则提供标明派生属性的最新进展快照，若尚不存在则说明暂无可用快照；与角色职责相关的当前委托和上下文入口；以及主持推荐查询的消息、事实记录或知识关键字。具体模板、顺序和表达形式尚未决定。

新角色不会自动接收完整历史，但可以在角色权限范围内主动查询并核对完整消息、事件和委托记录，以及可用的 thread 上下文或知识库。主持推荐的记录与关键字只表示相关性，不构成临时权限限制。

主持负责理解新角色的加入目的和职责，并语义判断哪些决定、委托、记录与知识线索相关。Harness 不理解内容或自行筛选重要历史，只依据主持选择、明确权威状态和角色权限提供、展示入场信息。本轮与 Dreaming 9 的完整记录/定向发送与权限边界、Dreaming 11 的确定性 Harness、Dreaming 13 的派生快照属性保持一致。

入场上下文的格式、协议、存储、文件布局、引用组装方式、上下文窗口使用和具体权限结构均未决定。

## Dreaming 15：每个 Thread 都有持续维护的知识检索层

确认每个 thread 都必须建立并持续维护知识检索层，为角色提供面向当前有效知识的查询能力。“持续”是贯穿 thread 生命周期的责任，不表示 Harness 逐条实时总结。完整消息、事件和委托记录仍是事实来源；这证明相应信息或行为发生过，不保证其中每个主张都正确。知识层只是基于记录形成的派生检索辅助，不会因为收录某项内容就自动创造事实、权威目标、已确认决定或审批结论，也不能覆盖完整记录。

所有角色都具有知识查询入口和动作的可用性，但实际可检索范围以及能否回看底层依据都受各自角色权限控制。知识查询是 thread 的公共协作能力，不等于所有角色能看到相同知识，也不把主持推荐的知识关键字变成临时访问限制。

知识维护可以由具备相应权限的角色主动触发，也可以按 Thread 配置通过时间、数量阈值或与进展快照同步等候选条件确定性触发，默认规则、权限、条件和范围尚未决定。触发后，Harness 通过专门的明确提示请求主持维护知识；主持负责语义判断，可以亲自完成或通过普通委托请其他角色代办。Harness 只发出请求并保存、展示结果，不理解内容，也不决定新增、修正、合并、过时或取代关系。

知识查询面向当前有效视图。知识可以被修正、合并、标记为过时或被新知识取代，但旧版本、演变过程和事实依据仍需保留并可追溯到完整记录。进展快照与知识层可以使用相似或同步的维护时机，但历史行为不同：快照保留每个检查点当时的视图且不回写，知识层持续维护当前有效内容，同时保留历史演变。

知识 schema、索引技术、存储、检索算法、维护提示词内容、默认触发、权限细目、无变化时行为，以及旧知识版本和演变关系的具体结构均未决定。

## Dreaming 16：多角色讨论沿用普通 Thread 协作能力

确认多角色讨论只是 thread 内普通协作的一种使用场景，不建立独立的讨论实体、活动、模式或生命周期。讨论目的、参与范围、互动方式、结束条件、进展记录和结果接收者等由角色通过自然语言规则表达，主持理解后使用普通消息、单一责任委托、既有路由与广播以及 Thread 配置推进；Harness 只按通用权限校验、路由、调用、并发和持久化逻辑执行，不识别特殊讨论模式，也不理解内容或判断讨论是否形成共识。

讨论产生的消息、委托和事件继续进入完整记录；其中的进展与风险可以由角色整理进进展快照，具有持续复用价值的认识可以进入知识检索层，但持久化或进入派生视图都不会自动创造权威结论。

主管理员不必发起或参与每次讨论。考虑到多角色讨论会产生 token 与 Agent 调用成本，主持能否自行发起由具备相应 Thread 权限的平台用户通过 Thread 配置控制；允许时主持可以自行组织并向主管理员同步结论。配置默认值、成本阈值、参与规模与同步格式尚未决定。

涉及重要风险的处置与其他重大治理事项仍由主管理员或在明确授权范围内的管理员决定；权威目标的设置、修改、替换和完成确认始终由主管理员唯一确认，不能从管理员的一般授权中自动推出。讨论 schema、状态、协议、算法、模板与调用流程均未决定。

## Dreaming 17：资源预算是 Thread 自治的强制边界

确认资源预算不是协作结束后的统计，而是由具备相应 Thread 配置权的平台用户设置、Harness 确定性计量并强制执行的自治边界。token、远程 API 费用、调用次数和运行时间是当前候选维度，正式维度及其定义、单位和组合方式尚未决定。

主持可以在预算与其他权限、配置允许的范围内自主选择协作方式，角色不能通过消息、委托或讨论自行修改或绕过平台预算。Harness 只计量消耗并与明确阈值比较，不判断一次调用是否值得，也不为主持选择业务方案。

软阈值触发对主持和相关用户角色的提醒，以便调整协作或由平台用户考虑预算变化；硬上限强制阻止新的成本型调用，直到平台用户追加预算或按照已配置规则获得明确授权。预算与具体能力开关可以组合：通用预算跨多类行为控制总资源，避免每种行为都必须单独配置，但预算余额不会自动开放被禁止的能力。

在途调用、估算误差、阈值默认值、恢复与一次性授权、按维度/角色/Agent 细分，以及成本数据不可得时的行为均未决定。本轮不设计预算 schema、计费适配器、计量算法、接口、协议或调用流程。

## Dreaming 18：自治程度由三项正交约束共同形成

确认自治程度是角色权限、审批策略和资源预算三项正交约束共同作用的结果，不是替代三者的单一等级或第四套授权。角色权限回答角色能否采取某类行动；审批策略回答某次行动执行前是否需要由谁批准；资源预算回答允许消耗的资源边界。一项行动必须同时满足全部适用约束，任一项不满足都不能因另外两项宽松而绕过。

具备相应 Thread 配置权的平台用户可以选择若干自治程度预设，也可以查看并逐项自定义真实约束。预设只是成组配置的便捷起点和表达，不产生额外权力、不遮蔽实际约束，也不阻止之后逐项调整；Harness 始终执行真实约束，而不是根据预设名称授权。

本轮明确修正 Dreaming 8 中“审批与自治程度”可能被理解为同一配置的旧表达：审批策略是三项真实约束之一，自治程度是三项约束共同形成的结果。Dreaming 8 原文继续保留，相关理解以 Dreaming 18 为准。

预设名称、数量、默认值、修改后的显示方式，以及审批动作分类、批准者、条件与范围均未决定。本轮不设计约束 schema、规则引擎、检查顺序、UI、接口、协议或调用流程。

## Dreaming 19：审批等待只阻塞发起角色的普通工作并暴露可用状态

确认一个角色发起审批请求并等待结果时，只对普通后续工作暂时不可用，不阻塞其他角色或整个 thread。Harness 可以依据明确的审批等待事实识别目标角色的可用状态，并向试图联系或安排该角色的发信者反馈；它只报告客观运行事实，不判断角色意愿、能力、繁忙程度或语义上的适合程度。

发信者得知目标不可用后，可以改找其他角色、处理其他事情，或明确选择等待。选择等待后，Harness 在目标恢复可用时通知发信者；恢复通知不等于原消息被自动重投，是否自动重投保持未决。

审批等待原则上不切断补充说明、调整、撤回等审批相关沟通，但其分类、权限、路由和完整行为复杂度较高，留待后续专门讨论。Harness 不能靠理解自由文本判断一条消息是否属于审批相关沟通。

本轮不设计完整审批模型、状态枚举、状态机、队列、并发竞态、UI、协议、模块或发布订阅实现。用户提出的状态变化事件、注册响应和发布订阅只作为未选择的实现线索记入延后设计记录。

## Dreaming 20：角色并发能力限制主动执行，不限制待处理责任

确认“同一角色同一时刻只有一个主动执行流”不是 Harness 对所有角色施加的统一规则，而是角色自身可能具有的并发能力限制。不同角色可以具备不同并发能力，Harness 应尊重各自边界，不把所有角色统一削弱为一次只能执行一项工作；并发能力来自身份卡还是角色实例、怎样表达及是否动态变化尚未决定。

角色并发能力只限制同时主动执行的工作数量。达到上限后，新的普通消息和委托仍可被接收并成为待处理内容，默认不因并发能力限制角色持有待处理消息和委托的数量；发信者应获知目标当前繁忙。接收不等于立即执行，Harness 不判断处理顺序或自动改派角色。

委托即使暂未开始执行，仍按照 Dreaming 10 立即形成由唯一目标角色承担并持续跟踪的责任。单纯繁忙也不同于 Dreaming 19 的审批等待：繁忙角色仍能接收普通后续内容，审批等待角色当前则对普通后续工作不可用。

本轮不决定能力来源、容量表达与动态变化、排队顺序、优先级、抢占、公平性、通知、超时、取消、调度、状态机、UI、协议或实现。

## Dreaming 21：待处理顺序由明确规则与可记录调整共同决定

确认角色释放主动执行能力后，下一项待处理内容由稳定、确定性的默认顺序规则与明确、可记录的语义调整共同决定。无特别指示时使用稳定默认顺序，但本轮不决定是否采用 FIFO；Harness 不读取内容后自行猜测重要性，也不因内容看似紧急而隐式改序。

主持或持有待处理内容的目标角色可以基于业务语义显式调整顺序。语义选择由 Agent 或真人完成，调整必须成为明确可记录行为；Harness 只应用调整结果，不理解或评价理由。谁可以调整哪些范围、多个调整如何组合与冲突仍未决定。

“待处理内容”只是普通消息和委托的集合性描述，不把两者合并成统一数据实体或队列。顺序调整只改变被考虑或开始处理的先后，不改变消息语义、委托责任、发布者、目标角色、验收权或生命周期，也不会自动取消、拒绝或转移委托。

本轮不决定具体默认顺序、优先级模型、调整权限范围、跨角色排序、饥饿与公平、抢占、自动启动、通知、UI、协议、状态机、算法或实现。

## Dreaming 22：主持顺序约束限定目标角色的调序自治

确认主持负责 thread 整体目标，因此主持明确设置且当前有效的待处理顺序约束优先；没有明确约束的范围内，目标角色可以基于专业判断显式调序。主持身份本身不会产生未表达的约束，Harness 也不猜测主持可能希望的顺序。

目标角色认为主持顺序存在依赖冲突、风险或明显低效时，应向主持说明并请求调整，不能静默覆盖。异议或请求本身不自动改变顺序；只有形成明确、有效的调整后 Harness 才应用新结果，在此之前继续使用当前约束。

主持可以明确把某个范围内的排序决定权交给目标角色，但这不转移主持理解整体目标和持续推进 thread 的责任，也不扩大目标角色其他权限、预算或审批自治。授权范围、校验、有效期、撤回与冲突行为尚未决定。

主持顺序优先和局部排序授权都不改变委托责任、发布者、唯一目标角色、验收权或生命周期，也不剥夺目标角色拒绝委托、请求调整委托等 Dreaming 10 既有行为。Harness 只应用明确约束、授权与有效结果，不理解异议理由。本轮不决定冲突协议、紧急例外、死锁、跨角色关系、UI、协议、状态机或实现。

## Dreaming 23：委托发布者提供排序输入，不自动获得跨工作排序权

确认委托发布者可以明确说明截止要求、紧急程度、依赖关系或建议优先级，并将其作为清楚记录、供主持和目标角色调序时考虑的重要输入。这些输入不会因为被发布者写入委托就自动成为已验证事实或具有约束力的顺序决定，Harness 也不理解或验证其语义。

截止要求可以属于委托内容或预期成果约束，但不会自动决定该委托相对目标角色其他待处理内容的执行顺序。普通发布者不能仅靠声明“最高优先级”单方面覆盖其他工作；Dreaming 10 中的取消权、验收权等单项委托权利也不会自动产生跨工作调序权。

最终顺序继续遵守 Dreaming 22。发布者同时是主持或另获相应排序决定权时，其要求只因该项身份或授权而在对应范围内具有约束力，而不是因为发布者身份。Harness 只应用明确、有效的排序约束或调整结果。本轮不决定字段、等级、截止执行与提醒、依赖验证、冲突、公平性、权限表达、UI、协议、状态机或实现。
